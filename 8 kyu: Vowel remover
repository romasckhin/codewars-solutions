Create a function called shortcut to remove all the lowercase vowels in a given string.

Examples
"hello"     -->  "hll"
"codewars"  -->  "cdwrs"
"goodbye"   -->  "gdby"
"HELLO"     -->  "HELLO"
Don't worry about uppercase vowels.

//
function shortcut(string){
  let a = string.split('').filter(el => el !== 'a' && el !== 'e' && el !== 'o' && el !== 'u' && el !== 'i')
  return a.join('')
}
